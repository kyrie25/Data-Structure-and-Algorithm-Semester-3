#pragma once

#ifndef UTILS_H
#define UTILS_H

#include <iostream>
#include <fstream>
#include <cmath>
#include <vector>
#include <time.h>

void GenerateData(std::vector<int> &arr, int dataType);

#endif // UTILS_H